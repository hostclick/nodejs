exports.config = {
  host: '127.0.0.1',
  port: 28778,

/*
  // Enable HTTP Basic Authentication
  auth: {
{% set passwd = '' %}
{% for stream in logio_basic_authentication %}
  user: "{{ stream.user }}",
{% if loop.last %}
{% set passwd = '"' + stream.passwd + '"' %}
{% else %}
{% set passwd = '"' + stream.passwd + '"' + ',' %}
{% endif %}
  pass: {{ passwd }}
{% endfor %}
},
*/

/* 
// Enable HTTPS/SSL
  ssl: {
    key: '/path/to/privatekey.pem',
    cert: '/path/to/certificate.pem'
  },

  // Restrict access to websocket (socket.io)
  // Uses socket.io 'origins' syntax
  restrictSocket: '*:*',

  // Restrict access to http server (express)
  restrictHTTP: [
    "192.168.29.39",
    "10.0.*"
  ]
*/

}
